// get input id and store it in a variable
let inputButton = document.getElementById('input-btn');
// Add Event Listener when clicked invoke function
inputButton.addEventListener('click', function () {
	console.log('Clicked!');
});
